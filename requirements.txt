# Production runtime dependencies -- pinned for reproducible numerics.
#
# These are kept in lock-step with the ranges declared in
# pyproject.toml [project.dependencies].  If you bump a range in
# pyproject.toml, regenerate this file with:
#
#     pip-compile pyproject.toml --extra=bench --extra=hardware
#
# and update the comment in pyproject.toml to point at the new pin.
# Do not hand-edit the version pins here without updating pyproject.
#
# Install with:  pip install -r requirements.txt

# Core numerics & physics
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
joblib>=1.3,<2.0

# Machine learning (the `bench` extra adds lightgbm, xgboost, catboost)
scikit-learn>=1.4,<2.0

# Ingestion & Telemetry Gateway
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
requests>=2.31,<3.0
h5py>=3.10,<4.0
# Serial hardware ingest (SerialSensorListener)
pyserial>=3.5,<4.0
# WebSocket transport layer (starlette/fastapi dependency)
websockets>=12,<15
